// Allow side-effect CSS imports (used by the Expo template for web styling).
// On native these are no-ops handled by Metro; this just satisfies TypeScript
// without depending on the generated expo-env.d.ts.
declare module '*.css';
